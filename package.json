{
  "scripts": {
    "dev": "npm run dev --prefix real-campus-voice-main",
    "build": "npm run build --prefix real-campus-voice-main",
    "preview": "npm run preview --prefix real-campus-voice-main"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.100.1"
  }
}
